let x: number = 10; // eslint-disable-line
x = 0b1010;

const y = 10;

const person = {
  name: 'Bruno' as const,
  lastName: 'Aparecido',
};
function colorRand(cor: 'vermelho' | 'amarelo' | 'azul'): string {
  return cor;
}
console.log(colorRand('vermelho'));
export default 1;
